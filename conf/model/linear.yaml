# @package _global_

# Inherit from base model configuration
defaults:
  - _base_
  - override /cv: default # Override with project defaults

model_type: linear

# Linear model doesn't need additional model_config parameters
model_config: {}
